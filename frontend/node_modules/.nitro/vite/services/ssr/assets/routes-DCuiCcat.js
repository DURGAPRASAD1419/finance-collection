import { s as useApp } from "./app-store-vl_DhqcK.js";
import { i as Field } from "./app-shell-WyGsWYV9.js";
import { useEffect, useState } from "react";
import { useNavigate } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { Lock, User } from "lucide-react";
//#region src/routes/index.tsx?tsr-split=component
function LoginPage() {
	const { login, loggedIn } = useApp();
	const navigate = useNavigate();
	const [username, setUsername] = useState("admin");
	const [password, setPassword] = useState("admin");
	const [error, setError] = useState("");
	useEffect(() => {
		if (loggedIn) navigate({ to: "/dashboard" });
	}, [loggedIn, navigate]);
	const submit = async (e) => {
		e.preventDefault();
		if (!username.trim() || !password.trim()) {
			setError("Enter username and password.");
			return;
		}
		try {
			if (!(await fetch(`https://finance-backend-fbbw.onrender.com/api/auth/login`, {
				method: "POST",
				headers: { "content-type": "application/json" },
				body: JSON.stringify({
					username,
					password
				})
			})).ok) {
				setError("Invalid credentials.");
				return;
			}
			login();
			navigate({ to: "/dashboard" });
		} catch (err) {
			setError("Unable to authenticate. Make sure the backend is running.");
		}
	};
	return /* @__PURE__ */ jsxs("div", {
		className: "mx-auto flex min-h-screen w-full max-w-md flex-col bg-background px-6",
		children: [/* @__PURE__ */ jsxs("div", {
			className: "pt-20",
			children: [
				/* @__PURE__ */ jsx("div", {
					className: "mb-8 flex size-16 items-center justify-center rounded-2xl bg-brand text-3xl font-bold text-brand-foreground",
					children: "₹"
				}),
				/* @__PURE__ */ jsx("h1", {
					className: "text-4xl font-bold text-primary",
					children: "LoanBook"
				}),
				/* @__PURE__ */ jsx("p", {
					className: "mt-2 text-base text-muted-foreground",
					children: "Collections, borrowers and reports in one place."
				})
			]
		}), /* @__PURE__ */ jsxs("form", {
			onSubmit: submit,
			className: "mt-12 space-y-7",
			children: [
				/* @__PURE__ */ jsx(Field, {
					label: "Username",
					required: true,
					children: /* @__PURE__ */ jsxs("div", {
						className: "relative",
						children: [/* @__PURE__ */ jsx("input", {
							className: "field-input pr-12",
							value: username,
							onChange: (e) => setUsername(e.target.value),
							placeholder: "admin"
						}), /* @__PURE__ */ jsx(User, { className: "absolute right-4 top-1/2 size-5 -translate-y-1/2 text-muted-foreground" })]
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "Password",
					required: true,
					children: /* @__PURE__ */ jsxs("div", {
						className: "relative",
						children: [/* @__PURE__ */ jsx("input", {
							className: "field-input pr-12",
							type: "password",
							value: password,
							onChange: (e) => setPassword(e.target.value),
							placeholder: "admin"
						}), /* @__PURE__ */ jsx(Lock, { className: "absolute right-4 top-1/2 size-5 -translate-y-1/2 text-muted-foreground" })]
					})
				}),
				error && /* @__PURE__ */ jsx("p", {
					className: "text-sm text-destructive",
					children: error
				}),
				/* @__PURE__ */ jsx("button", {
					type: "submit",
					className: "w-full rounded-lg bg-brand py-4 text-lg font-bold tracking-wide text-brand-foreground",
					children: "LOGIN"
				}),
				/* @__PURE__ */ jsx("p", {
					className: "text-center text-sm text-muted-foreground",
					children: "Forgot PIN? Contact your administrator."
				})
			]
		})]
	});
}
//#endregion
export { LoginPage as component };
