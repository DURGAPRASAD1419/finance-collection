import { s as useApp } from "./app-store-B1Qbf6V1.js";
import { i as Field } from "./app-shell-CRyAR8zm.js";
import { useState } from "react";
import { Link, useNavigate } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { ArrowLeft } from "lucide-react";
//#region src/routes/borrowers.new.tsx?tsr-split=component
function NewBorrowerPage() {
	const { addBorrower, notifySuccess, notifyError } = useApp();
	const navigate = useNavigate();
	const [form, setForm] = useState({
		name: "",
		fatherName: "",
		mobile: "",
		mobile2: "",
		work: "",
		address: ""
	});
	const set = (k) => (e) => setForm((f) => ({
		...f,
		[k]: e.target.value
	}));
	const submit = async (e) => {
		e.preventDefault();
		if (!form.name || !form.fatherName || !form.mobile || !form.work || !form.address) {
			notifyError("Please complete all required fields.");
			return;
		}
		try {
			await addBorrower(form);
			notifySuccess("Borrower added successfully.");
			navigate({ to: "/borrowers" });
		} catch (err) {
			console.error(err);
			notifyError("Unable to create borrower. Please try again.");
		}
	};
	return /* @__PURE__ */ jsxs("div", {
		className: "mx-auto min-h-screen w-full max-w-md bg-background pb-10",
		children: [/* @__PURE__ */ jsx("header", {
			className: "border-b border-border px-4 py-3",
			children: /* @__PURE__ */ jsx(Link, {
				to: "/borrowers",
				children: /* @__PURE__ */ jsx(ArrowLeft, { className: "size-6 text-brand" })
			})
		}), /* @__PURE__ */ jsxs("form", {
			onSubmit: submit,
			className: "space-y-7 px-4 pt-5",
			children: [
				/* @__PURE__ */ jsx("h1", {
					className: "text-2xl font-bold text-brand",
					children: "Create new borrower"
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "Name",
					required: true,
					children: /* @__PURE__ */ jsx("input", {
						className: "field-input",
						value: form.name,
						onChange: set("name")
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "Father Name",
					required: true,
					children: /* @__PURE__ */ jsx("input", {
						className: "field-input",
						value: form.fatherName,
						onChange: set("fatherName")
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "Mobile",
					required: true,
					children: /* @__PURE__ */ jsx("input", {
						className: "field-input",
						inputMode: "numeric",
						value: form.mobile,
						onChange: set("mobile")
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "Mobile2",
					children: /* @__PURE__ */ jsx("input", {
						className: "field-input",
						inputMode: "numeric",
						value: form.mobile2,
						onChange: set("mobile2")
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "Shop / Work",
					required: true,
					children: /* @__PURE__ */ jsx("input", {
						className: "field-input",
						value: form.work,
						onChange: set("work")
					})
				}),
				/* @__PURE__ */ jsx(Field, {
					label: "Address",
					required: true,
					children: /* @__PURE__ */ jsx("input", {
						className: "field-input",
						value: form.address,
						onChange: set("address"),
						placeholder: "Door number, street, city"
					})
				}),
				/* @__PURE__ */ jsx("button", {
					type: "submit",
					className: "w-full rounded-md bg-brand py-4 text-lg font-bold tracking-wide text-brand-foreground",
					children: "SAVE BORROWER"
				})
			]
		})]
	});
}
//#endregion
export { NewBorrowerPage as component };
