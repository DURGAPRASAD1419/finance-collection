import { createContext, useCallback, useContext, useEffect, useMemo, useState } from "react";
import { jsx } from "react/jsx-runtime";
//#region src/lib/api.ts
var API_BASE = typeof window !== "undefined" && window.location.hostname !== "localhost" ? "http://10.0.2.2:4000" : "http://localhost:4000";
var ApiError = class extends Error {
	status;
	message;
	details;
	constructor(status, message, details) {
		super(message);
		this.status = status;
		this.message = message;
		this.details = details;
	}
};
async function handleResponse(response) {
	if (!response.ok) {
		let errorMessage = `HTTP ${response.status}`;
		try {
			const data = await response.json();
			if (data.error) errorMessage = data.error;
		} catch {}
		throw new ApiError(response.status, errorMessage);
	}
	if (response.status === 204) return {};
	try {
		return await response.json();
	} catch {
		throw new ApiError(response.status, "Invalid response format from server");
	}
}
async function fetchWithRetry(url, options, maxRetries = 2) {
	let lastError;
	for (let attempt = 0; attempt <= maxRetries; attempt++) try {
		return await handleResponse(await fetch(url, options));
	} catch (error) {
		lastError = error instanceof ApiError ? error : new Error(String(error));
		if (lastError instanceof ApiError && lastError.status >= 400 && lastError.status < 500) throw lastError;
		if (attempt < maxRetries) await new Promise((resolve) => setTimeout(resolve, (attempt + 1) * 100));
	}
	throw lastError || /* @__PURE__ */ new Error("Failed to fetch after retries");
}
async function fetchBorrowers() {
	return fetchWithRetry(`${API_BASE}/api/borrowers`);
}
async function createBorrower(borrower) {
	return fetchWithRetry(`${API_BASE}/api/borrowers`, {
		method: "POST",
		headers: { "content-type": "application/json" },
		body: JSON.stringify(borrower)
	});
}
async function updateBorrower(id, borrower) {
	return fetchWithRetry(`${API_BASE}/api/borrowers/${id}`, {
		method: "PUT",
		headers: { "content-type": "application/json" },
		body: JSON.stringify(borrower)
	});
}
async function fetchLoans() {
	return fetchWithRetry(`${API_BASE}/api/loans`);
}
async function createLoan(loan) {
	return fetchWithRetry(`${API_BASE}/api/loans`, {
		method: "POST",
		headers: { "content-type": "application/json" },
		body: JSON.stringify(loan)
	});
}
async function recordPayment(payment) {
	return fetchWithRetry(`${API_BASE}/api/pay`, {
		method: "POST",
		headers: { "content-type": "application/json" },
		body: JSON.stringify(payment)
	});
}
async function exportDatabase() {
	const response = await fetch(`${API_BASE}/api/database/export`);
	if (!response.ok) throw new ApiError(response.status, "Failed to export database");
	return response.blob();
}
async function importDatabase(file) {
	const formData = new FormData();
	formData.append("file", file);
	return fetchWithRetry(`${API_BASE}/api/database/import`, {
		method: "POST",
		body: formData
	});
}
async function backupDatabase() {
	const response = await fetch(`${API_BASE}/api/database/backup`);
	if (!response.ok) throw new ApiError(response.status, "Failed to create backup");
	return response.blob();
}
//#endregion
//#region src/lib/app-store.tsx
var STORAGE_KEY = "loanbook-state-v1";
var Ctx = createContext(null);
var pad = (n) => String(n).padStart(2, "0");
function toDMY(d) {
	return `${pad(d.getDate())}/${pad(d.getMonth() + 1)}/${d.getFullYear()}`;
}
function fromDMY(s) {
	const [d, m, y] = s.split("/").map(Number);
	return new Date(y, m - 1, d);
}
function inr(n) {
	return `₹ ${Math.round(n).toLocaleString("en-IN")}`;
}
function addPeriod(date, frequency, n) {
	const d = new Date(date);
	if (frequency === "daily") d.setDate(d.getDate() + n);
	if (frequency === "weekly") d.setDate(d.getDate() + n * 7);
	if (frequency === "monthly") d.setMonth(d.getMonth() + n);
	return d;
}
function buildDues(start, frequency, installments, perInstallment) {
	const base = fromDMY(start);
	return Array.from({ length: installments }, (_, i) => ({
		no: i + 1,
		date: toDMY(addPeriod(base, frequency, i)),
		amount: perInstallment,
		paid: false
	}));
}
function loanTotals(loan) {
	const total = loan.dues.reduce((s, d) => s + d.amount, 0);
	const paid = loan.dues.filter((d) => d.paid).reduce((s, d) => s + d.amount, 0);
	const today = /* @__PURE__ */ new Date();
	return {
		total,
		paid,
		pending: loan.dues.filter((d) => !d.paid && fromDMY(d.date) <= today).reduce((s, d) => s + d.amount, 0),
		balance: total - paid,
		percent: total ? Math.round(paid / total * 100) : 0
	};
}
var initialState = {
	loggedIn: false,
	borrowers: [],
	loans: []
};
function AppProvider({ children }) {
	const [state, setState] = useState(initialState);
	const [hydrated, setHydrated] = useState(false);
	const [toasts, setToasts] = useState([]);
	useEffect(() => {
		async function load() {
			let persistedState = null;
			try {
				const raw = window.localStorage.getItem(STORAGE_KEY);
				if (raw) persistedState = JSON.parse(raw);
			} catch (error) {
				console.error("Failed to load from localStorage:", error);
			}
			try {
				const borrowers = await fetchBorrowers();
				const loans = await fetchLoans();
				setState({
					...initialState,
					...persistedState ?? {},
					borrowers,
					loans,
					loggedIn: persistedState?.loggedIn ?? false
				});
			} catch (error) {
				console.error("Failed to load data from API:", error);
				if (persistedState) setState({
					...initialState,
					...persistedState
				});
			}
			setHydrated(true);
		}
		load();
	}, []);
	useEffect(() => {
		if (!hydrated) return;
		window.localStorage.setItem(STORAGE_KEY, JSON.stringify(state));
	}, [state, hydrated]);
	const addToast = useCallback((message, type) => {
		const id = crypto.randomUUID();
		const toast = {
			id,
			message,
			type
		};
		setToasts((current) => [...current, toast]);
		window.setTimeout(() => setToasts((current) => current.filter((t) => t.id !== id)), 4e3);
	}, []);
	const notifySuccess = useCallback((message) => addToast(message, "success"), [addToast]);
	const notifyError = useCallback((message) => addToast(message, "error"), [addToast]);
	const removeToast = useCallback((id) => {
		setToasts((current) => current.filter((toast) => toast.id !== id));
	}, []);
	const login = useCallback(() => setState((s) => ({
		...s,
		loggedIn: true
	})), []);
	const logout = useCallback(() => setState((s) => ({
		...s,
		loggedIn: false
	})), []);
	const addBorrower = useCallback(async (b) => {
		const borrower = {
			...b,
			id: crypto.randomUUID()
		};
		try {
			const saved = await createBorrower(borrower);
			setState((s) => ({
				...s,
				borrowers: [...s.borrowers, saved]
			}));
			notifySuccess("Borrower saved successfully.");
			return saved;
		} catch (error) {
			const message = error instanceof ApiError ? `Failed to save borrower: ${error.message}` : "Unable to save borrower. Please check your connection and try again.";
			notifyError(message);
			throw error;
		}
	}, [notifyError, notifySuccess]);
	const updateBorrower$1 = useCallback(async (id, changes) => {
		try {
			const updated = await updateBorrower(id, changes);
			setState((s) => ({
				...s,
				borrowers: s.borrowers.map((b) => b.id === id ? updated : b)
			}));
			notifySuccess("Borrower updated successfully.");
			return updated;
		} catch (error) {
			const message = error instanceof ApiError ? `Failed to update borrower: ${error.message}` : "Unable to update borrower details. Please check your connection and try again.";
			notifyError(message);
			throw error;
		}
	}, [notifyError, notifySuccess]);
	const addLoan = useCallback(async (l) => {
		const prefix = l.frequency === "daily" ? "DL" : l.frequency === "weekly" ? "WL" : "ML";
		const count = state.loans.filter((x) => x.frequency === l.frequency).length + 1;
		const created = {
			...l,
			id: crypto.randomUUID(),
			code: `${prefix}-${count}`,
			dues: buildDues(l.startDate, l.frequency, l.installments, l.perInstallment)
		};
		try {
			const saved = await createLoan(created);
			setState((s) => ({
				...s,
				loans: [...s.loans, saved]
			}));
			notifySuccess("Loan approved and added successfully.");
			return saved;
		} catch (error) {
			const message = error instanceof ApiError ? `Failed to save loan: ${error.message}` : "Unable to save the loan. Please check your connection and try again.";
			notifyError(message);
			throw error;
		}
	}, [
		state.loans,
		notifyError,
		notifySuccess
	]);
	const payDue = useCallback(async (loanId, dueNo, paidDate, collectedBy) => {
		try {
			await recordPayment({
				loanId,
				dueNo,
				paidDate,
				collectedBy
			});
			setState((s) => ({
				...s,
				loans: s.loans.map((loan) => loan.id !== loanId ? loan : {
					...loan,
					dues: loan.dues.map((d) => d.no === dueNo ? {
						...d,
						paid: true,
						paidDate,
						collectedBy
					} : d)
				})
			}));
			notifySuccess("Payment recorded successfully.");
		} catch (error) {
			const message = error instanceof ApiError ? `Failed to record payment: ${error.message}` : "Unable to save payment. Please check your connection and try again.";
			notifyError(message);
			throw error;
		}
	}, [notifyError, notifySuccess]);
	const exportDatabase$1 = useCallback(async () => {
		try {
			const blob = await exportDatabase();
			const url = window.URL.createObjectURL(blob);
			const a = document.createElement("a");
			a.href = url;
			a.download = `loanbook-export-${(/* @__PURE__ */ new Date()).toISOString().slice(0, 10)}.db`;
			document.body.appendChild(a);
			a.click();
			window.URL.revokeObjectURL(url);
			document.body.removeChild(a);
			notifySuccess("Database exported successfully.");
		} catch (error) {
			const message = error instanceof ApiError ? `Export failed: ${error.message}` : "Unable to export database. Please try again.";
			notifyError(message);
		}
	}, [notifyError, notifySuccess]);
	const backupDatabase$1 = useCallback(async () => {
		try {
			const blob = await backupDatabase();
			const url = window.URL.createObjectURL(blob);
			const a = document.createElement("a");
			a.href = url;
			a.download = `loanbook-backup-${(/* @__PURE__ */ new Date()).toISOString().slice(0, 10)}.db`;
			document.body.appendChild(a);
			a.click();
			window.URL.revokeObjectURL(url);
			document.body.removeChild(a);
			notifySuccess("Backup created successfully.");
		} catch (error) {
			const message = error instanceof ApiError ? `Backup failed: ${error.message}` : "Unable to create backup. Please try again.";
			notifyError(message);
		}
	}, [notifyError, notifySuccess]);
	const importDatabase$1 = useCallback(async (file) => {
		try {
			const result = await importDatabase(file);
			notifySuccess(result.message);
			window.setTimeout(() => window.location.reload(), 2e3);
		} catch (error) {
			const message = error instanceof ApiError ? `Import failed: ${error.message}` : "Unable to import database. Please verify the file and try again.";
			notifyError(message);
		}
	}, [notifyError, notifySuccess]);
	const value = useMemo(() => ({
		...state,
		login,
		logout,
		addBorrower,
		updateBorrower: updateBorrower$1,
		addLoan,
		payDue,
		borrowerOf: (loan) => state.borrowers.find((b) => b.id === loan.borrowerId),
		notifySuccess,
		notifyError,
		toasts,
		removeToast,
		exportDatabase: exportDatabase$1,
		backupDatabase: backupDatabase$1,
		importDatabase: importDatabase$1
	}), [
		state,
		login,
		logout,
		addBorrower,
		updateBorrower$1,
		addLoan,
		payDue,
		notifySuccess,
		notifyError,
		removeToast,
		toasts,
		exportDatabase$1,
		backupDatabase$1,
		importDatabase$1
	]);
	return /* @__PURE__ */ jsx(Ctx.Provider, {
		value,
		children
	});
}
function useApp() {
	const ctx = useContext(Ctx);
	if (!ctx) throw new Error("useApp must be used inside AppProvider");
	return ctx;
}
//#endregion
export { loanTotals as a, inr as i, addPeriod as n, toDMY as o, fromDMY as r, useApp as s, AppProvider as t };
